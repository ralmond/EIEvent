Undocumented code objects:
  ‘copy’
  
  ‘setTimer’ ‘timer’ ‘timer<-’ ‘timerRunning’
  ‘timerRunning<-’ ‘timerTime’ ‘timerTime<-’ ‘

Undocumented S4 methods:
  generic 'setTimer' and siglist 'Status,character'
  generic 'timerRunning' and siglist 'Status,character'
  generic 'timerRunning<-' and siglist 'Status,character'
  generic 'timerTime' and siglist 'Status,character'
  generic 'timerTime<-' and siglist 'Status,character'

Functions to write:

testRule, testRuleScript
testPredicate, testPredicateScript

Classes to write:

EIEngine
RuleTable

Rule maintenance

Database Setup

Shiny page
